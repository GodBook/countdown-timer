修复视觉提醒：倒计时结束后显示真正的弹窗。应用在前台时显示应用内弹窗；使用其他应用时，授予悬浮窗权限后显示跨应用弹窗。

主界面选择视觉提醒后，可点击“允许后台弹窗”开启权限。未授权时保留通知提醒；锁屏或禁止悬浮窗的安全界面仍受系统限制。

“仅视觉提醒”弹窗不响铃；“视觉提醒＋响铃”可通过弹窗停止声音并关闭。返回应用时自动切换为应用内弹窗，避免重复显示。

1.1.0 用户可直接通过应用内“检查更新”升级，沿用原发行签名。
