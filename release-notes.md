修复“后台没有响铃和结束通知，回到应用才触发”的计时链路：三种提醒模式统一使用独立前台计时服务，不再依赖悬浮窗授权；精确闹钟直接启动服务，减少广播转发环节。

增加运行期间有超时保护的 CPU 唤醒锁及独立到时回调。暂停、重置、结束和服务销毁均释放唤醒锁；后台计时可能比旧版增加耗电。

新增“后台计时保护”和“允许后台持续计时”入口。iQOO/vivo 用户升级后请允许系统电池优化豁免，并在手机设置中允许自启动与后台运行；仅允许悬浮窗不等于允许后台计时。

新增本地“提醒诊断”：记录闹钟派发、服务启动和实际结束来源，可手动复制，不会自动上传。保留后台弹窗、通知控制、原发行签名和应用内更新。

本版在 Android 16 模拟器验证；未连接用户的 iQOO 真机，厂商后台限制仍需相应系统授权。
