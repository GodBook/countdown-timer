修复后台弹窗可靠性：选择视觉提醒时，在开始计时的前台阶段提前准备提醒服务，避免到时才从后台启动被系统拦截。

修复切换到桌面或其他应用瞬间结束时漏弹的问题；尚未确认的提醒会在应用退到后台后显示。服务被系统回收后可重新读取计时状态恢复。

增加“5 秒后台弹窗测试（返回桌面）”和真实服务状态提示。iQOO/vivo 设备提供后台权限设置提示与入口，窗口失败会记录诊断信息，不再直接忽略。

保留原来的悬浮窗权限和发行签名，1.1.0/1.1.1 用户可应用内升级。系统强制停止、锁屏和安全页面限制仍由系统控制。
